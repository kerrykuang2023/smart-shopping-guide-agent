PORT=8080
DEMO_MODE=true
VLM_PROVIDER=mock
RECOGNITION_CONFIDENCE_THRESHOLD=0.6

# 可选：运行时配置 JSON。Docker 部署建议在 compose 里设为 /app/data/runtime-settings.json 并挂载 ./data 卷
# RUNTIME_SETTINGS_FILE=/app/data/runtime-settings.json
